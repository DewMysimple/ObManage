# ObManage 工程约定

## 先读什么

本文件是项目的统一 Agent 指令入口。`AGENT.md` 引用本文件，避免两套规则分叉。开始工作时先查看 `git status -sb`，再按顺序阅读：

1. [工程记忆维护协议](wiki_memory/AGENTS.md)。
2. [项目概览](wiki_memory/当前状态/项目概览.md)、[系统架构](wiki_memory/当前状态/系统架构.md)、[当前约束](wiki_memory/当前状态/当前约束.md)、[当前待办](wiki_memory/当前状态/当前待办.md)。
3. 与任务相关的有效决策、知识页和已知问题；需要追溯时才读取最近 1–3 篇日志。

以当前源码、配置、测试与用户明确要求为依据。记忆是导航和结论，不替代事实核验，不默认加载全部历史。

## 产品与数据边界

- ObManage 是 Windows 中文 Obsidian 仓库管理工具，一个功能一个页面；仓库镜像固定为第一页，另含只读统计、限定子树部署和回收站清理。
- 仓库镜像每次只选一个方向，以来源为准同步新增、修改和删除；不做两端冲突合并。同步只能写入本次目标，来源必须保持不被复制、覆盖、改名或删除；目标独有内容属于删除范围。
- 普通镜像复制先暂存到目标盘、校验后替换；新增、更新失败或取消后必须停止后续删除。空来源清空目标需要手动确认。
- 统计全程只读；排除链接和重复嵌套统计，读取失败必须显示为不完整，不能静默给出精确结果。
- 配置和模板分发只处理明确选择的组件、目标与限定子树；分析零写，完整显示新增、更新、删除和结构变化。所有目标先同卷暂存校验，再原子切换；旧版本由持久事务记录绑定，用户明确撤销或确认保留后处理。
- 回收站清理只处理已验证仓库根目录下精确的 `.trash`。先把全部所选内容复制到独立隔离区并校验，预览仍有效后才清理；保留 `.trash` 本身，并支持持久恢复或最终清理隔离备份。
- 所有功能保留路径相等/包含保护、磁盘与目录身份复核、链接边界、长路径、陈旧预览检查和来源变化检查；持久记录不能单独扩大原授权写入范围。
- 任一部署或隔离事务待恢复、待确认或记录无法安全读取时，暂停所有新的镜像、定时镜像、部署和清理；只读操作及对应恢复动作可继续。应用自有 `.obmanage-deploy-*` 恢复目录不得进入仓库发现、统计或镜像。
- 开发测试使用隔离临时目录。真实用户仓库先做只读预览；除非用户明确要求，UI、文档和开发授权不包含执行真实镜像、部署或清理。
- 设置、SQLite 基线、事务日志、隔离备份和运行日志存放在本机应用数据目录，并与仓库分离；同卷原子切换所需的临时事务目录除外。

## 实现与界面

- Python 3.14、PySide6、SQLite；依赖版本以 requirements 文件为准。同步、统计、部署和清理业务引擎都不得依赖 Qt。
- 主窗口使用静态左侧导航和页面栈，一个功能一个页面，仓库镜像固定为第一页。新功能通过明确导入注册，保证 PyInstaller 冻结构建可发现，不使用运行时字符串插件导入。
- 保留应用全局单任务互斥及 `SyncWorker(QThread)` 生命周期；通用管理任务使用同一受控后台线程入口，避免将历史上不稳定的 worker/thread 模式重新引入。
- 路径行固定为本机和移动硬盘，方向决定实际来源/目标。切换方向或路径必须使旧预览、确认和定时绑定失效。
- 手动同步保留每轮方向确认；定时仅执行用户开启时绑定的方向和路径，忙碌时不排队补跑。
- UI 减少重复说明，为差异表留空间。各页面表格适应窗口宽度、保留列分隔线；长路径提供完整悬停和复制，删除位置与数量始终明确。离开页面会撤销该页的破坏性确认。
- 目标默认不选；分发前复核 Obsidian 打开仓库状态。多个持久批次都必须可选择，不能由较新的异常批次遮住旧恢复入口；删除事务备份需要独立确认，无法实际取消的阶段不显示取消按钮。
- 公共 README 面向首次接触产品的人；截图使用演示数据，勿提交用户仓库内容、私人路径、日志、凭据或本机配置。

## 验证与交付

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe wiki_memory\工具\memory_lint.py index
.\.venv\Scripts\python.exe wiki_memory\工具\memory_lint.py check
.\.venv\Scripts\python.exe tools\build.py
```

按改动范围选择测试：镜像变更验证两个方向和故障场景；统计验证严格只读、嵌套去重和错误可见；部署验证多目标预备、故障回滚、持久恢复和记录篡改 fail-closed；清理验证精确 `.trash` 边界、隔离、恢复、最终释放和部分失败重试。UI 变更检查每个页面在普通、小窗口及全屏尺寸，并验证表格宽度与完整路径可访问；纯文档检查链接和记忆格式即可。需要发布本地程序时重新打包并验证 EXE 启动，不只验证源码窗口。

打包必须保留 `tools/build.py` 的干净子进程 PATH，避免混入其他工具的 ICU DLL。重建前确认目标在本工作区 `dist/ObManage` 内，且旧 EXE 已退出。不要因打包而强制终止可能正在同步的程序。

## 每轮修改必须提交并推送

用户已明确要求：**每一次完整对话中有实质文件修改，结束前都要完成 Git 提交和推送。** 这项要求持续生效，无须每轮再次询问是否提交/推送；用户之后的新指示优先。

1. 检查当前分支、远程与工作区，保留用户已有修改。只暂存本轮任务范围内的文件，不盲目执行 `git add .`。
2. 完成必要验证，更新当前有效记忆；添加本轮工作日志并刷新索引、运行记忆检查。已明确授权的行为及已验证事实可直接记录；新的未确认重大方向保留为 proposed。
3. 检查待提交差异和敏感信息，使用描述最终变更的提交说明执行 `git commit`。
4. 推送到当前分支对应远程；新增分支使用 `codex/` 前缀，已有明确分支任务沿用当前分支。禁止擅自强推或重写公共历史。
5. 用 `git status -sb` 与远程分支提交哈希验证推送完成，在最终回复中给出提交标识和结果。日志不预先声称尚未发生的推送成功；推送凭据以远程引用为准。

如果推送失败，应处理可解决的问题并重试；需要用户介入时明确报告阻塞与当前提交状态，不宣称已推送。没有文件修改的纯讨论不创建空提交。
