---
type: log
status: archived
kind: maintenance
importance: high
updated: 2026-09-13
topic: flat-portable-archive
source_logs:
  - "[[日志/2026-09-13-最新便携包构建]]"
supersedes: null
---

# 2026-09-13｜压缩包根目录结构约束

- 时间：2026-09-13（北京时间）
- 类型：`maintenance`
- 状态：`已完成`
- 目标：把“每次完整对话的实质修改都提交、推送并生成最新 `ObManage.zip`”加入工程约束，并确保 ZIP 解压后直接就是程序根目录。
- 日志索引：[[日志/MOC_工作日志|工作日志 MOC]]

## 实现

- `tools/build.py` 在 PyInstaller `onedir` 构建完成并复制使用说明、第三方声明和许可证后，自动生成 `dist/ObManage.zip`。
- ZIP 使用程序目录内容作为归档根，根层直接包含 `ObManage.exe`、`_internal/`、`使用说明.md`、`THIRD_PARTY_NOTICES.md` 和 `licenses/`，不再产生 `ObManage/ObManage.exe` 的第二层目录。
- `AGENTS.md`、README、Windows 打包知识页、开发交付流程和 ADR-003 已同步该约束；构建产物仍留在 Git 忽略的 `dist/`。

## 验证

- `python tools/build.py`：成功生成便携目录与 ZIP；首次重建遇到旧烟测进程占用 `qwindows.dll`，正常关闭该进程后重试成功。
- `python -m pytest -q`：332 项通过，用时 59.74 秒。
- ZIP 共 236 个条目，存在根层 `ObManage.exe` 和 `_internal/`，不存在嵌套 `ObManage/ObManage.exe`。
- `dist/ObManage.zip` 大小 50,756,842 字节，SHA-256 为 `1BCF8C591E61727111C1F9C868CAC016D1DA3C22E7385453A8D8F0C52D2C6282`。
- EXE `--smoke-test` 启动并生成 `build/obmanage-package-smoke.png`；测试完成后无 ObManage 残留进程。
